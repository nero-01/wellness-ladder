"use client"

/** Route: `/auth/sign-up` — App Router uses `page.tsx` (not `sign-up.tsx`). */
import { SignUpView } from "@/components/auth/sign-up-view"

export default function AuthSignUpPage() {
  return <SignUpView />
}
