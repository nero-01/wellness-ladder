"use client"

import { SignUpView } from "@/components/auth/sign-up-view"

/** Alias for `/auth/sign-up` and `/sign-up` (no hyphen). */
export default function SignupAliasPage() {
  return <SignUpView />
}
