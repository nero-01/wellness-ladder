/**
 * Shared layout rhythm — screen insets, radii, and padding.
 * Use these instead of ad-hoc numbers so borders and whitespace stay consistent.
 */

/** Horizontal inset from device edges (scroll content, headers, cards) */
export const inset = 20

/** Standard card / catalog tile corner radius */
export const radiusMd = 18

/** Large section surfaces (profile blocks, home “Your Progress”) */
export const radiusLg = 20

/** Nested panels, chips, small controls */
export const radiusSm = 12

/** Inner blocks inside cards (task preview box, icon wells) */
export const radiusInner = 14

/** Default 1px UI frame */
export const hairline = 1

/** Interior padding for medium cards and strips */
export const padCard = 16

/** Padding for large section bodies */
export const padSection = 20

/** Vertical gap between major stacked sections */
export const gapSection = 16

/** Gap between related items (grid gap, horizontal card gap) */
export const gapItem = 12
